<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.asgmt.matwam">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity android:name=".module.feature.COVID19Activity"></activity>
        <activity android:name=".module.fact.OtherFactActivity" />
        <activity android:name=".module.shared.WebViewActivity" />
        <activity android:name=".module.investment.InvestmentListActivity" />
        <activity android:name=".module.fact.FactDetailActivity" />
        <activity android:name=".module.weather.WeatherListActivity" />
        <activity android:name=".module.weather.WeatherDetailActivity" />
        <activity android:name=".module.fact.FactListActivity" />
        <activity
            android:name=".module.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>